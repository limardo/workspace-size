# Workspaces size manager
manage your workspaces by command

## install script
clone the repo and run the ```install.sh```
```sh
./install.sh
```
prepend ```sudo``` if you are ubuntu's user
```sh
sudo ./install.sh
```